interface:
  display_name: "Video Agent"
  short_description: "Plan and build editable product videos with Agent tools"
  brand_color: "#0EA5E9"
  default_prompt: "Use $video-agent to inspect the project, plan a word-timed 9:16 product video, resolve or capture the right materials, and build an editable Jianying draft."

policy:
  allow_implicit_invocation: true
